"use client";

import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useState } from "react";
import { AppWindow, Boxes, ChevronRight, CircleUserRound, Database, Download, ImagePlus, Layers3, Library, Menu, Plus, Search, Settings2, ShieldCheck, Sparkles, Star, Upload, WandSparkles, X } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { Input } from "@/components/ui/input";
import { Progress } from "@/components/ui/progress";
import { Textarea } from "@/components/ui/textarea";
import { Toaster } from "@/components/ui/sonner";
import { toast } from "sonner";

export type StudioView="workspace"|"skill-editor"|"skill-market"|"model-market"|"datasets"|"project"|"profile"|"models"|"skill-detail";
const baseSteps=["SKU 原图保真修复","更换卧室场景背景","视觉分析与属性提取","标题、描述与标签","定价建议","生成 Etsy 上架包"];
const nav=[["/workspace","工作台",AppWindow],["/marketplace/skills","Skill 广场",Library],["/marketplace/models","模型广场",Boxes],["/datasets","数据清洗",Database],["/projects/demo","商品项目",Layers3],["/settings/models","模型设置",Settings2]] as const;
const skills=[
 {title:"床品场景图 · 轻法式卧室",author:"Mira Studio",rating:"4.9",runs:"2.8k",tag:"背景生成",color:"from-[#243d72] to-[#111b2e]"},
 {title:"商品图片保真清晰化",author:"LoomLink 官方",rating:"4.8",runs:"8.6k",tag:"官方",color:"from-[#28574f] to-[#102621]"},
 {title:"Etsy 标题与 13 标签",author:"NorthPeak",rating:"4.7",runs:"5.1k",tag:"文案",color:"from-[#593e76] to-[#21172d]"},
 {title:"Etsy 竞品数据清洗",author:"LoomLink 官方",rating:"4.9",runs:"3.2k",tag:"数据",color:"from-[#6a4928] to-[#271b11]"},
];
const download=(name:string,text:string,type="text/plain")=>{const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type}));a.download=name;a.click();URL.revokeObjectURL(a.href)};

function Brand(){return <Link href="/workspace" className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-primary text-white"><Layers3 className="size-5"/></span><span><b className="block text-[15px] text-white">织链 LoomLink</b><small className="text-xs text-slate-500">Commerce Skill Studio</small></span></Link>}
function Shell({children}:{children:React.ReactNode}){const path=usePathname(),router=useRouter();const[mobile,setMobile]=useState(false);const[q,setQ]=useState("");return <div className="min-h-screen text-slate-100"><aside className={`fixed inset-y-0 left-0 z-40 w-[248px] border-r border-white/8 bg-[#090d13]/95 px-4 py-5 backdrop-blur-xl transition-transform lg:translate-x-0 ${mobile?"translate-x-0":"-translate-x-full"}`}><div className="flex items-center justify-between px-2"><Brand/><Button aria-label="关闭菜单" variant="ghost" size="icon-sm" className="lg:hidden" onClick={()=>setMobile(false)}><X/></Button></div><nav className="mt-9 space-y-1">{nav.map(([href,label,Icon])=><Link key={href} href={href} onClick={()=>setMobile(false)} className={`flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm ${path.startsWith(href.replace("/demo",""))?"bg-[#172234] text-white":"text-slate-400 hover:bg-white/5 hover:text-white"}`}><Icon className="size-[18px]"/>{label}</Link>)}</nav><div className="absolute inset-x-4 bottom-5 rounded-2xl border border-white/8 bg-white/[.025] p-3"><div className="flex items-center gap-2 text-xs text-slate-400"><ShieldCheck className="size-4 text-emerald-400"/>密钥已加密保护</div><div className="mt-2 h-1.5 rounded-full bg-white/8"><div className="h-full w-2/3 rounded-full bg-primary"/></div><p className="mt-2 text-[11px] text-slate-600">演示环境 · 需配置真实模型</p></div></aside><div className="lg:pl-[248px]"><header className="sticky top-0 z-30 flex h-16 items-center gap-4 border-b border-white/8 bg-[#080b10]/80 px-4 backdrop-blur-xl md:px-7"><Button aria-label="打开菜单" variant="ghost" size="icon" className="lg:hidden" onClick={()=>setMobile(true)}><Menu/></Button><form className="relative max-w-xl flex-1" onSubmit={e=>{e.preventDefault();if(q.trim())router.push(`/marketplace/skills?q=${encodeURIComponent(q)}`)}}><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-500"/><Input value={q} onChange={e=>setQ(e.target.value)} className="h-9 border-white/8 bg-white/[.035] pl-9" placeholder="搜索 Skill、模型或项目…"/></form><Badge variant="outline" className="hidden border-amber-400/20 bg-amber-400/5 text-amber-200 sm:flex">演示模式</Badge><Link aria-label="个人主页" href="/profile/me" className="grid size-9 place-items-center rounded-full border border-white/10 bg-[#18243a]"><CircleUserRound className="size-5"/></Link></header><main className="grid-fade min-h-[calc(100vh-4rem)] p-4 md:p-7">{children}</main></div><Toaster richColors position="top-center"/></div>}

function Workspace(){const[files,setFiles]=useState<File[]>([]),[running,setRunning]=useState(false),[progress,setProgress]=useState(0),[extra,setExtra]=useState(false);function run(){if(!files.length){toast.error("请先上传至少一张 SKU 图片");return}setRunning(true);setProgress(10);const t=setInterval(()=>setProgress(v=>{if(v>=95){clearInterval(t);setRunning(false);toast.success("演示流程已完成，可在商品项目中查看结果");return 100}return v+17}),350)}return <div className="mx-auto max-w-[1480px]"><div className="mb-6 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[.18em] text-primary">商品生成工作台</p><h1 className="text-2xl font-semibold md:text-3xl">把一组 SKU，变成完整上架包</h1></div><div className="flex gap-2"><Button variant="outline" asChild><Link href="/projects/demo"><Download/>历史导出</Link></Button><Button asChild><Link href="/skills/new"><Plus/>创建 Skill</Link></Button></div></div><div className="grid gap-4 xl:grid-cols-[1fr_340px]"><section className="glass rounded-2xl p-4 md:p-6"><div className="grid gap-5 lg:grid-cols-2"><div><label className="text-sm font-medium">本次使用的 Skill</label><Link href="/skills/demo" className="mt-2 flex items-center justify-between rounded-xl border border-primary/30 bg-primary/5 p-3 hover:bg-primary/10"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-lg bg-primary/15 text-primary"><WandSparkles/></span><div><b className="text-sm">床品 Etsy 完整上架流程</b><p className="text-xs text-slate-500">6 步 · 图片 + 文案 + 定价</p></div></div><ChevronRight/></Link><div className="mt-5 grid grid-cols-2 gap-3"><Input aria-label="商品名称" defaultValue="法式水洗亚麻被套"/><Input aria-label="商品分类" defaultValue="Bedding / Duvet cover"/></div><Textarea className="mt-3 min-h-28 bg-black/20" defaultValue="天然亚麻触感，低饱和燕麦色。请保持织物纹理与颜色真实，生成适合 Etsy 的图片和英文文案。"/>{extra&&<Input className="mt-3" autoFocus placeholder="例如：背景不要出现人物"/>}<div className="mt-3 flex items-center justify-between"><Button variant="ghost" size="sm" onClick={()=>setExtra(v=>!v)}><Plus/>{extra?"收起补充要求":"添加步骤要求"}</Button><span className="text-xs text-slate-600">当前设备自动保存</span></div></div><div><label className="text-sm font-medium">上传 SKU 图片</label><label className="mt-2 flex min-h-64 cursor-pointer flex-col items-center justify-center rounded-2xl border border-dashed border-white/15 bg-black/20 p-6 text-center hover:border-primary/60"><input type="file" multiple accept="image/jpeg,image/png,image/webp" className="hidden" onChange={e=>{const f=Array.from(e.target.files||[]);setFiles(f);if(f.length)toast.success(`已载入 ${f.length} 张 SKU 图片`)}}/><Upload className="size-9 text-primary"/><b className="mt-4 text-sm">拖入文件夹或选择多张图片</b><p className="mt-1 text-xs text-slate-500">JPG、PNG、WebP · 单张不超过 20MB</p></label>{files.length>0&&<div className="mt-3 flex flex-wrap gap-2">{files.map(f=><Badge key={f.name} variant="secondary"><ImagePlus/>{f.name}</Badge>)}</div>}</div></div><div className="mt-6 border-t border-white/8 pt-5">{progress>0&&<div className="mb-4 rounded-xl bg-white/[.025] p-3"><div className="mb-2 flex justify-between text-xs"><span>{running?"正在执行商品流程":"流程已完成"}</span><b>{progress}%</b></div><Progress value={progress}/></div>}<div className="flex flex-wrap items-center justify-between gap-3"><p className="text-xs text-slate-500">演示运行不会产生模型费用；配置模型后可接入真实调用</p><Button size="lg" onClick={run} disabled={running}>{running?"生成中…":"开始生成上架包"}<ChevronRight/></Button></div></div></section><aside className="space-y-4"><div className="glass rounded-2xl p-4"><div className="flex justify-between"><b className="text-sm">流程步骤</b><Badge variant="secondary">6 步</Badge></div><div className="mt-4 space-y-1">{baseSteps.map((s,i)=><div key={s} className="flex items-center gap-3 rounded-xl p-2.5"><span className={`grid size-7 place-items-center rounded-lg text-xs ${i===0?"bg-primary":"bg-white/5 text-slate-500"}`}>{i+1}</span><span className="text-sm text-slate-300">{s}</span></div>)}</div></div><div className="overflow-hidden rounded-2xl border border-white/8 bg-[#10151d]"><img className="h-40 w-full object-cover opacity-80" src="https://images.unsplash.com/photo-1582224163312-0735047647c6?auto=format&fit=crop&q=75&w=900" alt="自然光下的亚麻床品"/><div className="p-4"><p className="text-xs text-slate-500">风格参考</p><b className="mt-1 block text-sm">自然光 · 真实亚麻质感</b></div></div></aside></div></div>}

function SkillEditor(){const[list,setList]=useState(baseSteps),[active,setActive]=useState(0),[prompt,setPrompt]=useState("保持 {product_name} 的颜色、织物纹理、尺寸比例和所有可见细节不变。根据 {sku_attributes} 生成自然、可信的电商结果。");const add=()=>{setList(v=>[...v,`新步骤 ${v.length+1}`]);setActive(list.length);toast.success("已添加新步骤")};async function publish(){const r=await fetch("/api/skills/demo/publish",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({steps:list,prompt})});const j=await r.json();j.ok?toast.success("发布检查通过，Skill 已发布"):toast.error(j.issues?.join("；")||"发布失败")}return <div className="mx-auto max-w-[1500px]"><div className="flex flex-wrap justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Skill Builder</p><h1 className="mt-2 text-3xl font-semibold">床品 Etsy 完整上架流程</h1></div><div className="flex gap-2"><Button variant="outline" onClick={()=>toast.success("草稿已保存")}>保存草稿</Button><Button onClick={publish}><ShieldCheck/>检查并发布</Button></div></div><div className="mt-6 grid gap-4 xl:grid-cols-[310px_1fr_320px]"><section className="glass rounded-2xl p-4"><div className="flex justify-between"><b className="text-sm">流程步骤</b><Button aria-label="添加步骤" size="icon-sm" variant="outline" onClick={add}><Plus/></Button></div><div className="mt-4 space-y-2">{list.map((s,i)=><button key={`${s}-${i}`} onClick={()=>setActive(i)} className={`w-full rounded-xl p-3 text-left ${active===i?"bg-primary/10 ring-1 ring-primary/40":"bg-white/[.025]"}`}><span className="mr-3 inline-grid size-7 place-items-center rounded-lg bg-white/5 text-xs">{i+1}</span><span className="text-sm">{s}</span></button>)}</div><Button variant="ghost" className="mt-3 w-full" onClick={add}><Plus/>添加步骤</Button></section><section className="glass rounded-2xl p-5"><b>步骤 {active+1} · {list[active]}</b><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm">输入来源<select className="mt-2 h-9 w-full rounded-md border bg-black/20 px-3"><option>上一步输出</option><option>SKU 原图</option></select></label><label className="text-sm">执行模型<select className="mt-2 h-9 w-full rounded-md border bg-black/20 px-3"><option>Gemini 3 Pro Image</option><option>GPT Image 2</option></select></label></div><label className="mt-5 block text-sm">通用提示词<Textarea value={prompt} onChange={e=>setPrompt(e.target.value)} className="mt-2 min-h-52 bg-black/20"/></label><div className="mt-3 flex flex-wrap gap-2">{["product_name","sku_name","sku_attributes","previous_text"].map(v=><button key={v} onClick={()=>setPrompt(p=>`${p} {${v}}`)}><Badge variant="outline">{`{${v}}`}</Badge></button>)}</div><div className="mt-5 flex justify-end gap-2"><Button variant="outline" onClick={()=>toast.info("单步试跑已加入队列")}>单步试跑</Button><Button onClick={()=>toast.success("完整流程试跑已开始")}><WandSparkles/>试跑完整流程</Button></div></section><aside className="glass h-fit rounded-2xl p-4"><b className="text-sm">发布规则</b><p className="mt-3 text-xs leading-6 text-slate-400">公开前自动检查 API Key、私有地址、敏感信息和缺失资源。保存草稿不会自动公开。</p></aside></div></div>}

function ModelsPanel(){const[baseUrl,setBaseUrl]=useState("https://api.example.com/v1"),[model,setModel]=useState("image-model-v1"),[key,setKey]=useState("");async function test(){toast.loading("正在检查连接…",{id:"model"});const r=await fetch("/api/models/test",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({baseUrl,modelId:model,apiKey:key})});const j=await r.json();j.ok?toast.success(`连接正常：${j.data.capabilities.join("、")}`,{id:"model"}):toast.error(j.error||"连接失败",{id:"model"})}return <div className="mx-auto max-w-6xl"><div className="flex justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Model Gateway</p><h1 className="mt-2 text-3xl font-semibold">模型与 API 密钥</h1><p className="mt-2 text-slate-400">保存真实密钥前请先配置托管环境的加密主密钥。</p></div><Button onClick={()=>toast.info("请在右侧填写新模型配置")}><Plus/>添加模型</Button></div><div className="mt-7 grid gap-4 lg:grid-cols-[1fr_360px]"><section className="space-y-3">{[["GPT Image 2","OpenAI 兼容"],["Gemini 3 Pro Image","Google Gemini"],["Claude Sonnet","Anthropic"]].map((m,i)=><div key={m[0]} className="glass flex items-center justify-between rounded-2xl p-4"><div><b>{m[0]}</b><p className="text-xs text-slate-500">{m[1]} · 示例配置</p></div><Button variant="outline" size="sm" onClick={()=>{setModel(m[0]);toast.info(`已载入 ${m[0]} 配置`)}}>编辑</Button></div>)}</section><aside className="glass rounded-2xl p-5"><b>连接测试</b><label className="mt-5 block text-sm">Base URL<Input value={baseUrl} onChange={e=>setBaseUrl(e.target.value)} className="mt-2"/></label><label className="mt-4 block text-sm">Model ID<Input value={model} onChange={e=>setModel(e.target.value)} className="mt-2"/></label><label className="mt-4 block text-sm">API Key<Input value={key} onChange={e=>setKey(e.target.value)} className="mt-2" type="password" placeholder="仅用于本次测试"/></label><Button className="mt-5 w-full" onClick={test}><ShieldCheck/>检查连接与能力</Button></aside></div></div>}

function DatasetPanel(){const[file,setFile]=useState<File|null>(null),[rows,setRows]=useState<string[][]>([]);async function clean(){if(!file)return;const text=await file.text();const parsed=text.replace(/^\uFEFF/,"").split(/\r?\n/).filter(Boolean).slice(0,1000).map(r=>r.split(","));setRows(parsed.slice(1));toast.success(`已读取 ${Math.max(0,parsed.length-1)} 行数据`)}return <div className="mx-auto max-w-6xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">Data Cleaner</p><h1 className="mt-2 text-3xl font-semibold">Etsy 竞品数据清洗</h1><p className="mt-2 text-slate-400">导入 Landrop CSV，预览并下载清洗结果。</p><div className="mt-7 grid gap-4 lg:grid-cols-[380px_1fr]"><section className="glass rounded-2xl p-5"><label className="flex min-h-52 cursor-pointer flex-col items-center justify-center rounded-2xl border border-dashed border-white/15 bg-black/20 p-6 text-center"><input type="file" accept=".csv,text/csv" className="hidden" onChange={e=>{const f=e.target.files?.[0]||null;setFile(f);if(f)toast.success(`已选择 ${f.name}`)}}/><Database className="size-9 text-primary"/><b className="mt-4">{file?file.name:"选择 Landrop CSV"}</b></label><Button className="mt-4 w-full" disabled={!file} onClick={clean}>开始清洗</Button>{rows.length>0&&<Button variant="outline" className="mt-2 w-full" onClick={()=>download("cleaned-etsy-data.csv",rows.map(r=>r.join(",")).join("\n"),"text/csv")}><Download/>下载 CSV</Button>}</section><section className="glass rounded-2xl p-5"><div className="grid grid-cols-3 gap-3">{[[String(rows.length),"有效行"],[String(new Set(rows.map(r=>r[5])).size),"独立店铺"],[rows.length?"已完成":"待导入","状态"]].map(x=><div key={x[1]} className="rounded-xl bg-white/[.035] p-4"><b className="text-xl">{x[0]}</b><p className="mt-1 text-xs text-slate-500">{x[1]}</p></div>)}</div><div className="mt-5 overflow-auto rounded-xl border"><table className="w-full min-w-[520px] text-left text-sm"><thead className="bg-white/5"><tr><th className="p-3">字段 1</th><th>字段 2</th><th>字段 3</th><th>字段 4</th></tr></thead><tbody>{rows.slice(0,8).map((r,i)=><tr key={i} className="border-t"><td className="max-w-48 truncate p-3">{r[0]}</td><td>{r[1]}</td><td>{r[2]}</td><td>{r[3]}</td></tr>)}</tbody></table></div></section></div></div>}

function Marketplace({view}:{view:StudioView}){const router=useRouter();const titles:Record<string,[string,string]>={"skill-market":["Skill 广场","发现和使用社区公开流程。"],"model-market":["模型广场","选择公开模型配置，填入自己的 API Key。"],project:["商品项目","查看生成记录和下载上架包。"],profile:["Mira Studio","管理个人资料和公开 Skill。"],"skill-detail":["床品场景图 · 轻法式卧室","查看流程版本、评价并开始使用。"]};const[t,s]=titles[view];return <div className="mx-auto max-w-6xl"><p className="text-xs font-semibold uppercase tracking-[.18em] text-primary">LoomLink</p><div className="mt-2 flex justify-between gap-4"><div><h1 className="text-3xl font-semibold">{t}</h1><p className="mt-2 text-slate-400">{s}</p></div><Button onClick={()=>view==="project"?download("listing.csv","title,description,price\nFrench linen duvet,Washed linen bedding,89.00","text/csv"):router.push(view==="model-market"?"/settings/models":"/skills/new")}>{view==="project"?<><Download/>下载上架包</>:<><Plus/>新建</>}</Button></div><div className="mt-8 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{skills.map((x,i)=><Link href={view==="model-market"?"/settings/models":view==="project"?"/projects/demo":`/skills/${i+1}`} key={x.title} className="glass overflow-hidden rounded-2xl transition hover:-translate-y-1"><div className={`h-28 bg-gradient-to-br ${x.color} p-4`}><Badge variant="secondary">{x.tag}</Badge></div><div className="p-4"><h2 className="font-medium">{view==="model-market"?["GPT Image 2","Gemini 3 Pro Image","Claude Sonnet","自定义 OpenAI 兼容"][i]:x.title}</h2><p className="mt-1 text-xs text-slate-500">{x.author}</p><div className="mt-4 flex justify-between text-xs text-slate-400"><span className="flex gap-1"><Star className="size-3.5 fill-amber-300 text-amber-300"/>{x.rating}</span><span>{x.runs} 次使用</span></div></div></Link>)}</div></div>}

export function StudioApp({view}:{view:StudioView}){return <Shell>{view==="workspace"?<Workspace/>:view==="skill-editor"?<SkillEditor/>:view==="models"?<ModelsPanel/>:view==="datasets"?<DatasetPanel/>:<Marketplace view={view}/>}</Shell>}
