const privateHosts=[/^localhost$/i,/^127\./,/^10\./,/^192\.168\./,/^169\.254\./,/^172\.(1[6-9]|2\d|3[01])\./,/^\[?::1\]?$/];
export function validatePublicBaseUrl(value:string){const u=new URL(value);if(u.protocol!=="https:")throw new Error("Base URL 必须使用 HTTPS");if(u.username||u.password)throw new Error("URL 不得包含凭据");if(privateHosts.some(r=>r.test(u.hostname)))throw new Error("不允许本机或私网地址");for(const k of u.searchParams.keys())if(/key|token|secret|auth/i.test(k))throw new Error("URL 不得包含敏感参数");u.hash="";u.pathname=u.pathname.replace(/\/+$/,"");return u.toString();}
export function redactSecrets(value:string){return value.replace(/(sk-[a-z0-9_-]{8,}|bearer\s+[a-z0-9._-]{8,}|api[_-]?key["'=:\s]+)[^\s"']+/gi,"[REDACTED]");}
export function publicationIssues(input:string){const issues:string[]=[];if(/sk-[a-z0-9_-]{8,}/i.test(input))issues.push("检测到疑似 API Key");if(/localhost|127\.0\.0\.1|192\.168\./i.test(input))issues.push("检测到私有地址");if(!input.trim())issues.push("Skill 定义为空");return issues;}
export async function encryptSecret(secret:string,keyText:string){const enc=new TextEncoder();const keyBytes=await crypto.subtle.digest("SHA-256",enc.encode(keyText));const key=await crypto.subtle.importKey("raw",keyBytes,"AES-GCM",false,["encrypt"]);const iv=crypto.getRandomValues(new Uint8Array(12));const data=new Uint8Array(await crypto.subtle.encrypt({name:"AES-GCM",iv},key,enc.encode(secret)));return `${btoa(String.fromCharCode(...iv))}.${btoa(String.fromCharCode(...data))}`;}
